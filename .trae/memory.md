# Agent 永久记忆

## 协作约定

1. **后续 commit 必须用中文写。**
   - 提交信息标题和正文均使用中文。
   - 遵循 conventional commit 风格时，类型可用英文（如 `feat:`、`fix:`、`ci:`），但描述部分用中文。

2. **禁止创建临时分支。**
   - 所有改动直接在用户指定的目标分支上进行。
   - 不在当前仓库中创建 `trae/agent-*` 等隔离分支。
   - 若当前会话被自动切到临时分支，应切换回目标分支并在目标分支上完成修改。
